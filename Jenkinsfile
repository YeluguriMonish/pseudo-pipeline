node {
    stage('Preparation') {
        def world = [[name: "john", age:23], [name: "tim", age: 42]]
        world.each { target ->
        echo "$target.name"
        echo "$target.age"
        }
    }
}
